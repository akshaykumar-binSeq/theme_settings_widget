name: theme_settings_widget
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flex_seed_scheme: ^1.4.0
  flutter:
    sdk: flutter
  get: ^4.6.5
  hive_flutter: ^1.1.0
  toggle_switch: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
